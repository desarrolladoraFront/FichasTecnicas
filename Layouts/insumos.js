export const createInsumos = ( id, arr ) => {
    // revisa que realmente haya entrado con un id de insumo
    if (id === 1) {
        //toma el main para manipular el dom e incluir el resto de elementos.
       const main = document.getElementById('main');

       //crea los elementos:
        

    }
}